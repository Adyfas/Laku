{
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./styles/index.css -o ./styles/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./styles/index.css -o ./styles/output.css --minify",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "tailwindcss": "^4.3.2"
  }
}
